def calculate_area(width, height):
    # Receive two parameters
    area = width * height # Multiply then
    return area # Returns the result

def is_even(number):
    # Receive an parameter
    return number % 2 == 0 # Calculate the remainder of multiply

# Print the variable name with the Print function
name = "Bob"
print("Welcome, " + name)

def square_numbers(numbers):
    # Create an empty list to store squared numbers
    squared = [] # Array
    for number in numbers:
        # run the array 
        square_number = number ** 2
        # if true, add the number in the array 
        square.append(squared_number)
        # return the array with the numbers
    return squared
